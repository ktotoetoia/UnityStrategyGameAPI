using TDS.Events;
using UnityEngine;

namespace TDS.Entities
{
    public class EntitiesTracking : MonoBehaviour
    {
        public void Init(IEntityRegister entityRegister, IEventSubscriber events)
        {
        }
    }
}